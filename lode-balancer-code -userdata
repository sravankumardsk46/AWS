----------------------------------------------------------------------------------- this is shows all details of load ----------------------------------
#!/bin/bash
sudo apt-get update -y
sudo apt-get install apache2 unzip -y

echo '<html><center><body bgcolor="black" text="#39ff14" style="font-family: Arial">
<h1>@BecomeDevops</h1>

<h1>Load Balancer Demo</h1><h3>Availability Zone: ' > /var/www/html/index.html
curl http://169.254.169.254/latest/meta-data/placement/availability-zone >> /var/www/html/index.html

echo '</h3> <h3>Instance Id: ' >> /var/www/html/index.html
curl http://169.254.169.254/latest/meta-data/instance-id >> /var/www/html/index.html

echo '</h3> <h3>Public IP: ' >> /var/www/html/index.html
curl http://169.254.169.254/latest/meta-data/public-ipv4 >> /var/www/html/index.html

echo '</h3> <h3>Local IP: ' >> /var/www/html/index.html
curl http://169.254.169.254/latest/meta-data/local-ipv4 >> /var/www/html/index.html

echo '</h3></html>' >> /var/www/html/index.html

#################################################################################### ec2 one #######################################################
#!/bin/bash

# Update packages and install Apache and curl
apt-get update -y
apt-get install -y apache2 curl

# Enable Apache on boot and start it
systemctl enable apache2
systemctl start apache2

# Fetch instance metadata
INSTANCE_ID=$(curl -s http://169.254.169.254/latest/meta-data/instance-id)
PUBLIC_IP=$(curl -s http://169.254.169.254/latest/meta-data/public-ipv4)
PRIVATE_IP=$(curl -s http://169.254.169.254/latest/meta-data/local-ipv4)
AZ=$(curl -s http://169.254.169.254/latest/meta-data/placement/availability-zone)
REGION=$(curl -s http://169.254.169.254/latest/dynamic/instance-identity/document | grep region | awk -F\" '{print $4}')

# Create a simple HTML page with instance metadata
cat <<EOF > /var/www/html/index.html
<!DOCTYPE html>
<html>
<head>
  <title>EC2 Metadata - Apache</title>
</head>
<body>
  <h1>EC2 Instance Metadata</h1>
  <ul>
    <li><strong>Instance ID:</strong> $INSTANCE_ID</li>
    <li><strong>Public IP:</strong> $PUBLIC_IP</li>
    <li><strong>Private IP:</strong> $PRIVATE_IP</li>
    <li><strong>Availability Zone:</strong> $AZ</li>
    <li><strong>Region:</strong> $REGION</li>
  </ul>
</body>
</html>
EOF

# Restart Apache to ensure it's serving the new file
systemctl restart apache2
######################################################################################## end #########################################################################
-------------------------------------------------------------- ec2 nginx ----------------------------------------------------------------------------------------------
#!/bin/bash

# Update system and install NGINX and curl
apt-get update -y
apt-get install -y nginx curl

# Enable and start NGINX
systemctl enable nginx
systemctl start nginx

# Fetch EC2 instance metadata
INSTANCE_ID=$(curl -s http://169.254.169.254/latest/meta-data/instance-id)
PUBLIC_IP=$(curl -s http://169.254.169.254/latest/meta-data/public-ipv4)
PRIVATE_IP=$(curl -s http://169.254.169.254/latest/meta-data/local-ipv4)
AZ=$(curl -s http://169.254.169.254/latest/meta-data/placement/availability-zone)
REGION=$(curl -s http://169.254.169.254/latest/dynamic/instance-identity/document | grep region | awk -F\" '{print $4}')

# Create a simple HTML file with the instance metadata
cat <<EOF > /var/www/html/index.html
<!DOCTYPE html>
<html>
<head>
  <title>EC2 Metadata - NGINX</title>
</head>
<body>
  <h1>EC2 Instance Metadata</h1>
  <ul>
    <li><strong>Instance ID:</strong> $INSTANCE_ID</li>
    <li><strong>Public IP:</strong> $PUBLIC_IP</li>
    <li><strong>Private IP:</strong> $PRIVATE_IP</li>
    <li><strong>Availability Zone:</strong> $AZ</li>
    <li><strong>Region:</strong> $REGION</li>
  </ul>
</body>
</html>
EOF

# Restart NGINX to apply changes
systemctl restart nginx
____________________________________________________________________- end __________________________________________________________________________

                                                      NGINX LODA BALANCER EC2 ONE 


#!/bin/bash
# Update and install NGINX and unzip
apt-get update -y
apt-get install -y nginx unzip curl

# Enable and start NGINX
systemctl enable nginx
systemctl start nginx

# Create HTML page with instance metadata (same look as Apache version)
cat <<EOF > /var/www/html/index.html
<html>
  <center>
    <body bgcolor="black" text="#39ff14" style="font-family: Arial">
      <h1>@sravankumar</h1>
      <h1>Load Balancer Demo one</h1>
      <h3>Availability Zone: $(curl -s http://169.254.169.254/latest/meta-data/placement/availability-zone)</h3>
      <h3>Instance Id: $(curl -s http://169.254.169.254/latest/meta-data/instance-id)</h3>
      <h3>Public IP: $(curl -s http://169.254.169.254/latest/meta-data/public-ipv4)</h3>
      <h3>Local IP: $(curl -s http://169.254.169.254/latest/meta-data/local-ipv4)</h3>
    </body>
  </center>
</html>
EOF

# Restart NGINX
systemctl restart nginx



-------------------------------------------------------------------------------------------------------------------------



                                        APACHE2 LOAD BALNCER EC2 TWO

#!/bin/bash

# Log all output (for debugging if needed)
exec > /var/log/user-data.log 2>&1

# Update packages and install Apache
apt-get update -y
apt-get install -y apache2 curl

# Enable and start Apache
systemctl enable apache2
systemctl start apache2

# Wait for metadata service to be ready
sleep 5

# Generate HTML with embedded metadata
cat <<EOF > /var/www/html/index.html
<html>
  <head>
    <title>EC2 Metadata</title>
  </head>
  <body bgcolor="black" text="#39ff14" style="font-family: Arial; text-align: center;">
    <h1>@sravankumar</h1>
    <h1>Load Balancer Demo TWO</h1>
    <h3>Availability Zone: $(curl -s http://169.254.169.254/latest/meta-data/placement/availability-zone)</h3>
    <h3>Instance Id: $(curl -s http://169.254.169.254/latest/meta-data/instance-id)</h3>
    <h3>Public IP: $(curl -s http://169.254.169.254/latest/meta-data/public-ipv4)</h3>
    <h3>Local IP: $(curl -s http://169.254.169.254/latest/meta-data/local-ipv4)</h3>
  </body>
</html>
EOF

# Restart Apache to ensure updated page is served
systemctl restart apache2


